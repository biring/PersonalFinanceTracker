package model;

public enum AccountType {
    CREDIT,  // For Checking accounts (typically credit type means + is deposit, - is withdrawal)
    DEBIT    // For Mortgage or Credit Card accounts (typically debit type means - is deposit, + is withdrawal)
}
